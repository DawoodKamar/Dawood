---
phase: 02-layout-navigation
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/pages/index.astro
autonomous: false
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05]

must_haves:
  truths:
    - "Homepage displays a hero section with headline, subheadline, and CTA linking to /blog"
    - "Homepage displays a positioning section with 2-3 paragraphs of body text"
    - "Homepage displays 4 content pillars: AI & Work, Focus & Discipline, Culture & Place, Clarity & Communication"
    - "Homepage displays the most recent non-draft blog post cards with title, description, date, reading time, and link"
    - "Homepage displays a newsletter signup section with email input and submit button (placeholder, not wired)"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Full homepage with all 5 sections wired to live collection data"
      contains: "getCollection"
  key_links:
    - from: "src/pages/index.astro"
      to: "astro:content"
      via: "getCollection and render imports"
      pattern: 'import.*getCollection.*render.*from.*astro:content'
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "layout wrapper"
      pattern: "import BaseLayout"
    - from: "src/pages/index.astro"
      to: "src/content/blog/*.md"
      via: "collection query with draft filter"
      pattern: "getCollection.*blog"
---

<objective>
Build all five homepage sections (hero, positioning, content pillars, featured posts, newsletter placeholder) in index.astro, wired to live blog collection data for the featured posts section. After this plan, the homepage is fully rendered and visually complete.

Purpose: The homepage is the primary landing page — it needs to showcase content and guide visitors to the blog. The featured posts section demonstrates the content collection integration from Phase 1 is working end-to-end.
Output: Fully rendered index.astro with 5 sections; npm run build exits 0.
</objective>

<execution_context>
@/Users/dawoodkamar/projects/Dawood/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dawoodkamar/projects/Dawood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout-navigation/02-RESEARCH.md
@.planning/phases/02-layout-navigation/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01: BaseLayout props interface -->
From src/layouts/BaseLayout.astro:
```typescript
interface Props {
  title: string;
  description?: string;
}
```

<!-- Content collection schema from Phase 1 -->
From src/content.config.ts:
```typescript
const blog = defineCollection({
  loader: glob({ pattern: "**/*.{md,mdx}", base: "./src/content/blog" }),
  schema: z.object({
    title: z.string(),
    description: z.string(),
    date: z.coerce.date(),
    category: z.enum([
      "AI & Work",
      "Focus & Discipline",
      "Culture & Place",
      "Clarity & Communication",
    ]),
    tags: z.array(z.string()).default([]),
    draft: z.boolean().default(false),
  }),
});
```

From src/config.ts:
```typescript
export const SITE_URL = "https://dawoodkamar.com";
export const SITE_TITLE = "Dawood Kamar";
```

<!-- Sample posts available (from Phase 1) -->
- src/content/blog/ai-noise.md — published, category: "AI & Work"
- src/content/blog/focus-advantage.md — published, category: "Focus & Discipline"
- src/content/blog/switzerland-ambition.md — draft: true (excluded in prod)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build all five homepage sections with live collection data</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (current state — should already use BaseLayout from Plan 02-01)
    - src/layouts/BaseLayout.astro (verify Props interface and slot usage)
    - src/content.config.ts (verify schema field names: title, description, date, category, tags, draft)
    - src/content/blog/ai-noise.md (verify frontmatter structure matches schema)
    - src/config.ts (SITE_TITLE for hero)
  </read_first>
  <action>
Rewrite `src/pages/index.astro` with all five homepage sections. The page wraps in `<BaseLayout title="Dawood Kamar" description="Writing on AI, focus, and meaningful work.">`.

**Frontmatter:**
```astro
---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection, render } from "astro:content";

const allPosts = await getCollection("blog", ({ data }) =>
  import.meta.env.PROD ? data.draft !== true : true
);

const featuredPosts = allPosts
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);

const postsWithReadingTime = await Promise.all(
  featuredPosts.map(async (post) => {
    const { remarkPluginFrontmatter } = await render(post);
    return { post, minutesRead: remarkPluginFrontmatter.minutesRead };
  })
);
---
```

CRITICAL: Use `data.date` NOT `data.pubDate`. The schema field is `date`. Use `render` imported from `astro:content` (standalone function, NOT `post.render()`).

**Section 1 — Hero (HOME-01):**
```html
<section class="py-16 text-center md:py-24">
  <h1 class="text-4xl font-bold tracking-tight md:text-5xl lg:text-6xl">
    Dawood Kamar
  </h1>
  <p class="mx-auto mt-4 max-w-2xl text-lg text-gray-600 dark:text-gray-400 md:text-xl">
    Writing on AI, focus, and building a meaningful career.
  </p>
  <a
    href="/blog/"
    class="mt-8 inline-block rounded-lg bg-blue-600 px-6 py-3 text-sm font-semibold text-white hover:bg-blue-700"
  >
    Read the Blog
  </a>
</section>
```
- CTA must link to `/blog/` (trailing slash)
- CTA must be an `<a>` tag with `href="/blog/"`

**Section 2 — Positioning (HOME-02):**
```html
<section class="mx-auto max-w-2xl py-12">
  <h2 class="text-2xl font-bold md:text-3xl">What I Write About</h2>
  <div class="mt-6 space-y-4 text-gray-600 dark:text-gray-400">
    <p>
      I explore how ambitious professionals can use AI as a thinking partner — not a replacement — and why focus is the real competitive advantage in a noisy world.
    </p>
    <p>
      My writing draws from experience across cultures and industries: growing up in South Africa, living in Switzerland, and working at the intersection of technology and business transformation.
    </p>
    <p>
      This is a space for people who want to think clearly, work intentionally, and build something that matters.
    </p>
  </div>
</section>
```

**Section 3 — Content Pillars (HOME-03):**
Create a 4-column grid (1 col mobile, 2 col tablet, 4 col desktop). Each pillar is a card with title and description.

The 4 pillars with exact content:
1. Title: "AI & Work" — Description: "How to use AI as a thinking partner, cut through the hype, and stay ahead without burning out."
2. Title: "Focus & Discipline" — Description: "Building systems for deep work, managing energy, and making progress on what actually matters."
3. Title: "Culture & Place" — Description: "Lessons from living across continents — how place shapes ambition, perspective, and identity."
4. Title: "Clarity & Communication" — Description: "Writing clearly, thinking precisely, and communicating ideas that move people to action."

Grid markup:
```html
<section class="py-12">
  <h2 class="text-2xl font-bold md:text-3xl">Content Pillars</h2>
  <div class="mt-8 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
    <!-- Repeat for each pillar -->
    <div class="rounded-lg border border-gray-200 p-6 dark:border-gray-800">
      <h3 class="font-semibold">{pillar.title}</h3>
      <p class="mt-2 text-sm text-gray-600 dark:text-gray-400">{pillar.description}</p>
    </div>
  </div>
</section>
```

**Section 4 — Featured Posts (HOME-04):**
Map over `postsWithReadingTime` array. Each post card shows:
- Title as `<h3>` wrapped in `<a href={"/blog/" + post.id + "/"}>` 
- Description from `post.data.description`
- Date formatted with `post.data.date.toLocaleDateString("en-GB", { day: "numeric", month: "short", year: "numeric" })`
- Reading time from `minutesRead`
- Category tag from `post.data.category`

Card markup for each post:
```html
<article class="rounded-lg border border-gray-200 p-6 dark:border-gray-800">
  <div class="flex items-center gap-2 text-sm text-gray-500 dark:text-gray-400">
    <time datetime={post.data.date.toISOString()}>
      {post.data.date.toLocaleDateString("en-GB", { day: "numeric", month: "short", year: "numeric" })}
    </time>
    <span>·</span>
    <span>{minutesRead}</span>
  </div>
  <h3 class="mt-2 text-lg font-semibold">
    <a href={`/blog/${post.id}/`} class="hover:text-blue-600 dark:hover:text-blue-400">
      {post.data.title}
    </a>
  </h3>
  <p class="mt-2 text-gray-600 dark:text-gray-400">{post.data.description}</p>
  <span class="mt-3 inline-block rounded-full bg-gray-100 px-3 py-1 text-xs font-medium text-gray-700 dark:bg-gray-800 dark:text-gray-300">
    {post.data.category}
  </span>
</article>
```

Section wrapper:
```html
<section class="py-12">
  <h2 class="text-2xl font-bold md:text-3xl">Latest Writing</h2>
  <div class="mt-8 grid gap-6 md:grid-cols-2 lg:grid-cols-3">
    {postsWithReadingTime.map(({ post, minutesRead }) => (
      <!-- card markup above -->
    ))}
  </div>
</section>
```

Note: Currently only 2 non-draft posts exist. The `.slice(0, 3)` will return 2 posts gracefully — this is fine.

**Section 5 — Newsletter Placeholder (HOME-05):**
This is a PLACEHOLDER form. Do NOT wire Buttondown API — that is Phase 3 scope (NEWS-01 through NEWS-04 are deferred).

```html
<section class="rounded-lg bg-gray-50 py-12 text-center dark:bg-gray-900">
  <h2 class="text-2xl font-bold md:text-3xl">Stay in the Loop</h2>
  <p class="mx-auto mt-4 max-w-lg text-gray-600 dark:text-gray-400">
    Writing on AI, focus, and meaningful work. No noise.
  </p>
  <form class="mx-auto mt-6 flex max-w-md gap-2">
    <input
      type="email"
      placeholder="your@email.com"
      required
      disabled
      class="flex-1 rounded-lg border border-gray-300 px-4 py-2 text-sm disabled:cursor-not-allowed disabled:opacity-50 dark:border-gray-700 dark:bg-gray-800"
    />
    <button
      type="submit"
      disabled
      class="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white disabled:cursor-not-allowed disabled:opacity-50"
    >
      Coming Soon
    </button>
  </form>
  <p class="mt-3 text-sm text-gray-500 dark:text-gray-400">Newsletter launching soon.</p>
</section>
```

After building all sections, run `npm run build` to verify the page compiles with the collection query.
  </action>
  <verify>
    <automated>cd /Users/dawoodkamar/projects/Dawood && grep -q "getCollection" src/pages/index.astro && grep -q 'href="/blog/"' src/pages/index.astro && grep -q "AI &amp; Work\|AI & Work" src/pages/index.astro && grep -q "Focus &amp; Discipline\|Focus & Discipline" src/pages/index.astro && grep -q "Culture &amp; Place\|Culture & Place" src/pages/index.astro && grep -q "Clarity &amp; Communication\|Clarity & Communication" src/pages/index.astro && grep -q 'type="email"' src/pages/index.astro && grep -q "remarkPluginFrontmatter" src/pages/index.astro && PATH="/opt/homebrew/opt/node@22/bin:$PATH" npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/index.astro imports `getCollection` and `render` from `astro:content`
    - File imports `BaseLayout` from `../layouts/BaseLayout.astro` and wraps all content in `<BaseLayout>`
    - Hero section: contains `<h1>` with "Dawood Kamar" and `<a>` with `href="/blog/"`
    - Positioning section: contains at least 2 `<p>` tags with body text
    - Content pillars section: contains all 4 pillar titles — "AI & Work", "Focus & Discipline", "Culture & Place", "Clarity & Communication"
    - Featured posts section: uses `postsWithReadingTime.map` to render post cards
    - Post cards contain `post.data.title`, `post.data.description`, `post.data.date`, `minutesRead`, `post.data.category`
    - Post card links use `href={`/blog/${post.id}/`}` pattern
    - Newsletter section: contains `type="email"` input with `disabled` attribute
    - Newsletter submit button has `disabled` attribute and text "Coming Soon" or "Coming soon"
    - File does NOT contain `pubDate` (must use `date` field name)
    - File does NOT contain `buttondown` or any API endpoint URL
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Homepage renders all 5 sections: hero with CTA to /blog/, positioning text, 4 content pillar cards, featured post cards from live collection data with reading time, and disabled newsletter placeholder form. npm run build exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify homepage layout and navigation in browser</name>
  <what-built>Complete site shell (BaseLayout with ClientRouter, Navigation with hamburger and dark mode toggle, Footer with social links) and full homepage with 5 sections (hero, positioning, content pillars, featured posts from live data, newsletter placeholder).</what-built>
  <how-to-verify>
    1. Run `npm run dev` (or use the dev server if already running)
    2. Open http://localhost:4321/ in browser
    3. Verify hero section shows "Dawood Kamar" headline and "Read the Blog" CTA button
    4. Verify positioning section shows 2-3 paragraphs of text
    5. Verify content pillars section shows 4 cards: AI & Work, Focus & Discipline, Culture & Place, Clarity & Communication
    6. Verify featured posts section shows 2 post cards (only 2 non-draft posts exist) with title, description, date, reading time, and category tag
    7. Verify newsletter section shows disabled email input and "Coming Soon" button
    8. Verify navigation bar shows Home, Blog, Contact links — Home should be highlighted as active
    9. Verify footer shows copyright, nav links, YouTube and LinkedIn links
    10. Click the dark mode toggle — page should switch to dark theme
    11. Refresh the page — dark mode should persist (no flash of light content)
    12. Navigate to a different page (e.g., /blog/) and back — dark mode should persist with no FOUC
    13. On mobile viewport (toggle in DevTools): verify hamburger menu appears and opens/closes correctly
    14. Navigate away and back — verify hamburger still works (astro:page-load re-initializes)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 — all templates compile and collection queries succeed
2. Homepage contains all 5 sections verified by grep for key content strings
3. Featured posts use live collection data (getCollection import present)
4. Newsletter form is placeholder only (disabled inputs, no API endpoint)
5. Visual verification in browser confirms layout, dark mode, and navigation work
</verification>

<success_criteria>
- Homepage displays hero with CTA, positioning text, 4 content pillars, featured post cards from live data, and newsletter placeholder
- Featured posts show title, description, date, reading time, and category from content collection
- Newsletter section is visually complete but non-functional (disabled, no Buttondown wiring)
- All content wrapped in BaseLayout
- npm run build exits 0
- Human verification confirms visual layout and interactive features work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-navigation/02-02-SUMMARY.md`
</output>
