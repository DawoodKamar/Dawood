---
phase: 02-layout-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - src/components/Navigation.astro
  - src/components/Footer.astro
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, NAV-06, NAV-07]

must_haves:
  truths:
    - "Every page wraps in BaseLayout with ClientRouter providing SPA-style navigation"
    - "Dark mode persists across page navigations without any flash of light content"
    - "Dark mode persists across browser refresh without flash"
    - "Hamburger menu opens and closes on every page, including after navigating away and back"
    - "Active nav link is highlighted on each page via aria-current=page"
    - "Footer with copyright, nav links, and social links appears on every page"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Base layout wrapping all pages with ClientRouter and dark mode FOUC prevention"
      contains: "ClientRouter"
    - path: "src/components/Navigation.astro"
      provides: "Nav links, active link highlighting, hamburger menu, dark mode toggle"
      contains: "astro:page-load"
    - path: "src/components/Footer.astro"
      provides: "Copyright, nav links, social links"
      contains: "youtube"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Navigation.astro"
      via: "import and render"
      pattern: "import Navigation"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Footer.astro"
      via: "import and render"
      pattern: "import Footer"
    - from: "src/layouts/BaseLayout.astro"
      to: "astro:transitions"
      via: "ClientRouter import"
      pattern: 'import.*ClientRouter.*from.*astro:transitions'
    - from: "src/components/Navigation.astro"
      to: "astro:page-load event"
      via: "event listener for hamburger and theme toggle"
      pattern: 'addEventListener.*astro:page-load'
---

<objective>
Build the persistent site shell: BaseLayout.astro with ClientRouter for view transitions and dark mode FOUC prevention, Navigation.astro with active link highlighting and mobile hamburger menu, and Footer.astro with social links. After this plan, every page can use BaseLayout and get navigation, footer, dark mode, and view transitions for free.

Purpose: Establishes the layout wrapper that every page in the site depends on. Without this, no page can be built with proper navigation or transitions.
Output: Three component files — BaseLayout.astro, Navigation.astro, Footer.astro — all wired and building cleanly.
</objective>

<execution_context>
@/Users/dawoodkamar/projects/Dawood/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dawoodkamar/projects/Dawood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout-navigation/02-RESEARCH.md

<interfaces>
<!-- Existing files the executor needs to understand -->

From src/styles/global.css:
```css
@import "tailwindcss";
@plugin "@tailwindcss/typography";
@custom-variant dark (&:where(.dark, .dark *));
```

From src/config.ts:
```typescript
export const SITE_URL = "https://dawoodkamar.com";
export const SITE_TITLE = "Dawood Kamar";
```

From astro.config.mjs:
```javascript
import { defineConfig } from "astro/config";
import tailwindcss from "@tailwindcss/vite";
import { remarkReadingTime } from "./src/plugins/remark-reading-time.mjs";

export default defineConfig({
  output: "static",
  site: "https://dawoodkamar.com",
  vite: { plugins: [tailwindcss()] },
  markdown: { remarkPlugins: [remarkReadingTime] },
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BaseLayout.astro with ClientRouter and dark mode FOUC prevention</name>
  <files>src/layouts/BaseLayout.astro</files>
  <read_first>
    - src/pages/index.astro (current placeholder page structure)
    - src/styles/global.css (Tailwind v4 import pattern and dark mode variant)
    - src/config.ts (SITE_TITLE constant)
    - astro.config.mjs (verify no conflicting config)
  </read_first>
  <action>
Create `src/layouts/BaseLayout.astro` with the following exact structure:

**Frontmatter:**
```astro
---
import { ClientRouter } from "astro:transitions";
import Navigation from "../components/Navigation.astro";
import Footer from "../components/Footer.astro";
import "../styles/global.css";

interface Props {
  title: string;
  description?: string;
}

const { title, description = "Dawood Kamar — writing on AI, focus, and work." } = Astro.props;
---
```

**Template:**
- `<html lang="en">` as root element (no class attribute — dark class applied dynamically via JS)
- `<head>` containing:
  - `<meta charset="utf-8" />`
  - `<meta name="viewport" content="width=device-width, initial-scale=1" />`
  - `<title>{title}</title>`
  - Conditional `<meta name="description" content={description} />` when description is truthy
  - `<ClientRouter />` component (per NAV-01, NAV-04 — this enables view transitions with default fade and graceful fallback)
  - An `<script is:inline>` block (per NAV-06) containing exactly:
    ```javascript
    (function () {
      var theme = localStorage.getItem("theme");
      if (theme === "dark") {
        document.documentElement.classList.add("dark");
      }
    })();
    document.addEventListener("astro:before-swap", function (event) {
      var theme = localStorage.getItem("theme");
      if (theme === "dark") {
        event.newDocument.documentElement.classList.add("dark");
      }
    });
    ```
  - CRITICAL: Use `astro:before-swap` NOT `astro:after-swap`. Use `event.newDocument.documentElement` NOT `document.documentElement`. This prevents FOUC.
- `<body>` containing:
  - `<Navigation />` component
  - `<main>` wrapping `<slot />` (the page content)
  - `<Footer />` component

**Tailwind styling on body:** `class="min-h-screen bg-white text-gray-900 dark:bg-gray-950 dark:text-gray-100"` to establish light/dark base colors.

**Tailwind styling on main:** `class="mx-auto max-w-4xl px-4 py-8"` for centered content container.
  </action>
  <verify>
    <automated>cd /Users/dawoodkamar/projects/Dawood && grep -q "ClientRouter" src/layouts/BaseLayout.astro && grep -q "astro:before-swap" src/layouts/BaseLayout.astro && grep -q "event.newDocument.documentElement" src/layouts/BaseLayout.astro && grep -q "is:inline" src/layouts/BaseLayout.astro && grep -q "Navigation" src/layouts/BaseLayout.astro && grep -q "Footer" src/layouts/BaseLayout.astro && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/BaseLayout.astro exists and contains `import { ClientRouter } from "astro:transitions"`
    - File contains `<ClientRouter />` in the head section
    - File contains `<script is:inline>` block with `astro:before-swap` (NOT `astro:after-swap`)
    - The before-swap handler uses `event.newDocument.documentElement.classList.add("dark")` (NOT `document.documentElement`)
    - File imports and renders `Navigation` and `Footer` components
    - File imports `../styles/global.css`
    - Props interface declares `title: string` and `description?: string`
    - Body has dark mode classes: `dark:bg-gray-950 dark:text-gray-100`
  </acceptance_criteria>
  <done>BaseLayout.astro exists with ClientRouter, FOUC-prevention inline script using astro:before-swap on event.newDocument, Navigation and Footer imports, and dark mode base styles</done>
</task>

<task type="auto">
  <name>Task 2: Create Navigation.astro and Footer.astro components</name>
  <files>src/components/Navigation.astro, src/components/Footer.astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro (just created — verify import paths match)
    - src/styles/global.css (dark mode variant pattern)
    - src/config.ts (SITE_TITLE for brand name in nav)
  </read_first>
  <action>
**Create `src/components/Navigation.astro`** (per NAV-02, NAV-05, NAV-07):

Frontmatter:
```astro
---
import { SITE_TITLE } from "../config";
const currentPath = Astro.url.pathname;
const navLinks = [
  { href: "/", label: "Home" },
  { href: "/blog/", label: "Blog" },
  { href: "/contact/", label: "Contact" },
];
---
```

Template structure:
- `<header class="border-b border-gray-200 dark:border-gray-800">` wrapping a `<nav class="mx-auto flex max-w-4xl items-center justify-between px-4 py-4">`
- Brand link: `<a href="/" class="text-xl font-bold">{SITE_TITLE}</a>`
- Desktop nav: `<ul class="hidden items-center gap-6 md:flex">` containing li items for each navLink
- Each nav link `<a>` must have:
  - `href={link.href}`
  - `aria-current={currentPath === link.href ? "page" : undefined}` (for active state per NAV-02)
  - Active class: `class:list={["hover:text-blue-600 dark:hover:text-blue-400", { "font-semibold text-blue-600 dark:text-blue-400": currentPath === link.href }]}`
- **Special case for homepage:** The homepage path is `/` which must match exactly. Other paths use trailing slash (`/blog/`, `/contact/`). Use `currentPath === link.href` for all comparisons — the trailing slash is already in the navLinks array.
- Dark mode toggle button: `<button id="theme-toggle" type="button" aria-label="Toggle dark mode" class="rounded-lg p-2 hover:bg-gray-100 dark:hover:bg-gray-800">` containing a sun/moon icon (use two spans: one with `class="dark:hidden"` showing a moon emoji or SVG, one with `class="hidden dark:inline"` showing a sun emoji or SVG — use text characters if no icon library: "🌙" and "☀️")
- Hamburger button: `<button id="menu-toggle" type="button" class="md:hidden" aria-expanded="false" aria-controls="mobile-menu" aria-label="Toggle menu">` containing a hamburger icon (three horizontal bars using spans or SVG)
- Mobile menu: `<div id="mobile-menu" class="border-b border-gray-200 md:hidden dark:border-gray-800" hidden>` containing a `<ul class="space-y-2 px-4 py-4">` with the same nav links (same active logic) plus a dark mode toggle

**Script block** (per NAV-05 — must use `astro:page-load`, NOT `DOMContentLoaded`):
```html
<script>
  document.addEventListener("astro:page-load", () => {
    const menuToggle = document.getElementById("menu-toggle");
    const mobileMenu = document.getElementById("mobile-menu");
    const themeToggle = document.getElementById("theme-toggle");
    const mobileThemeToggle = document.getElementById("mobile-theme-toggle");

    if (menuToggle && mobileMenu) {
      menuToggle.addEventListener("click", () => {
        const expanded = menuToggle.getAttribute("aria-expanded") === "true";
        menuToggle.setAttribute("aria-expanded", String(!expanded));
        mobileMenu.hidden = expanded;
      });
    }

    function toggleTheme() {
      const isDark = document.documentElement.classList.toggle("dark");
      localStorage.setItem("theme", isDark ? "dark" : "light");
    }

    if (themeToggle) {
      themeToggle.addEventListener("click", toggleTheme);
    }
    if (mobileThemeToggle) {
      mobileThemeToggle.addEventListener("click", toggleTheme);
    }
  });
</script>
```

**Create `src/components/Footer.astro`** (per NAV-03):

Template:
- `<footer class="border-t border-gray-200 dark:border-gray-800">` wrapping a `<div class="mx-auto flex max-w-4xl flex-col items-center gap-4 px-4 py-8 md:flex-row md:justify-between">`
- Copyright: `<p class="text-sm text-gray-500 dark:text-gray-400">&copy; {new Date().getFullYear()} Dawood Kamar. All rights reserved.</p>`
- Nav links: `<nav class="flex gap-4 text-sm">` with links to `/`, `/blog/`, `/contact/` styled `class="hover:text-blue-600 dark:hover:text-blue-400"`
- Social links: `<div class="flex gap-4">` containing:
  - YouTube: `<a href="https://youtube.com/@dawoodkamar" target="_blank" rel="noopener noreferrer" aria-label="YouTube">YouTube</a>`
  - LinkedIn: `<a href="https://linkedin.com/in/dawoodkamar" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn">LinkedIn</a>`
  - Both styled `class="text-sm text-gray-500 hover:text-blue-600 dark:text-gray-400 dark:hover:text-blue-400"`

After creating both components, delete the throwaway `src/pages/test-reading-time.astro` (per locked decision from Phase 1).

Then update `src/pages/index.astro` to use BaseLayout so the build succeeds:
```astro
---
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout title="Dawood Kamar">
  <div class="prose dark:prose-invert">
    <h1>Dawood Kamar</h1>
    <p>Site under construction.</p>
  </div>
</BaseLayout>
```

Finally, run `npm run build` to verify everything compiles.
  </action>
  <verify>
    <automated>cd /Users/dawoodkamar/projects/Dawood && grep -q "astro:page-load" src/components/Navigation.astro && grep -q "aria-current" src/components/Navigation.astro && grep -q "menu-toggle" src/components/Navigation.astro && grep -q "theme-toggle" src/components/Navigation.astro && grep -q "youtube" src/components/Footer.astro && grep -q "linkedin" src/components/Footer.astro && grep -q "BaseLayout" src/pages/index.astro && test ! -f src/pages/test-reading-time.astro && PATH="/opt/homebrew/opt/node@22/bin:$PATH" npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Navigation.astro exists and contains `document.addEventListener("astro:page-load"` (NOT `DOMContentLoaded`)
    - Navigation.astro contains `aria-current={currentPath === link.href ? "page" : undefined}` or equivalent
    - Navigation.astro contains `id="menu-toggle"` for hamburger button
    - Navigation.astro contains `id="theme-toggle"` for dark mode toggle
    - Navigation.astro contains `id="mobile-menu"` div with `hidden` attribute
    - Navigation.astro contains `localStorage.setItem("theme"` in toggle function
    - src/components/Footer.astro exists and contains `youtube` (case-insensitive link)
    - Footer.astro contains `linkedin` (case-insensitive link)
    - Footer.astro contains `&copy;` or `©` for copyright
    - src/pages/index.astro imports BaseLayout and wraps content in `<BaseLayout>`
    - src/pages/test-reading-time.astro is DELETED (must not exist)
    - `npm run build` exits 0 with no errors
  </acceptance_criteria>
  <done>Navigation.astro with astro:page-load listener, active links via aria-current, hamburger menu, and dark mode toggle; Footer.astro with copyright, nav links, YouTube and LinkedIn social links; index.astro updated to use BaseLayout; test-reading-time.astro deleted; npm run build exits 0</done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 — confirms all imports resolve and templates compile
2. `grep -q "ClientRouter" src/layouts/BaseLayout.astro` — ClientRouter is present
3. `grep -q "astro:before-swap" src/layouts/BaseLayout.astro` — FOUC prevention uses correct event
4. `grep -q "astro:page-load" src/components/Navigation.astro` — scripts use correct lifecycle event
5. `grep -q "aria-current" src/components/Navigation.astro` — active link highlighting exists
6. `test ! -f src/pages/test-reading-time.astro` — throwaway page deleted
</verification>

<success_criteria>
- BaseLayout.astro wraps pages with ClientRouter for view transitions and dark mode FOUC prevention
- Navigation.astro provides active link highlighting, hamburger menu, and dark mode toggle, all using astro:page-load
- Footer.astro provides copyright, nav links, and YouTube/LinkedIn social links
- test-reading-time.astro from Phase 1 is deleted
- index.astro uses BaseLayout
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-navigation/02-01-SUMMARY.md`
</output>
