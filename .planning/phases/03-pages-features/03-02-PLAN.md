---
phase: 03-pages-features
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/pages/contact.astro
  - src/components/ContactForm.astro
  - src/pages/success.astro
  - src/components/Footer.astro
  - src/pages/index.astro
autonomous: false
requirements: [CONTACT-01, CONTACT-02, CONTACT-03, CONTACT-04, CONTACT-05, CONTACT-06, NEWS-04]

must_haves:
  truths:
    - "Visiting /contact shows a contact form with Name, Email, and Message fields"
    - "The contact form has data-netlify='true' and data-astro-reload attributes"
    - "Submitting the contact form redirects to /success"
    - "The /success page thanks the user and links back to home"
    - "A honeypot field named bot-field exists on the contact form"
    - "Client-side validation prevents submission of empty fields and invalid emails"
    - "Social links (YouTube, LinkedIn) appear on the contact page"
    - "NewsletterSignup component appears in the site footer on every page"
    - "NewsletterSignup component appears on the homepage (replacing disabled placeholder)"
    - "NewsletterSignup component appears in at least 3 locations total (homepage, post footer, site footer)"
  artifacts:
    - path: "src/pages/contact.astro"
      provides: "Contact page with form and social links"
      contains: "ContactForm"
    - path: "src/components/ContactForm.astro"
      provides: "Netlify Forms component"
      contains: "data-netlify"
    - path: "src/pages/success.astro"
      provides: "Form submission success page"
      contains: "BaseLayout"
    - path: "src/components/Footer.astro"
      provides: "Updated footer with NewsletterSignup"
      contains: "NewsletterSignup"
    - path: "src/pages/index.astro"
      provides: "Updated homepage with live NewsletterSignup"
      contains: "NewsletterSignup"
  key_links:
    - from: "src/pages/contact.astro"
      to: "src/components/ContactForm.astro"
      via: "import and render"
      pattern: "import ContactForm"
    - from: "src/components/ContactForm.astro"
      to: "/success/"
      via: "form action attribute"
      pattern: 'action="/success/"'
    - from: "src/components/ContactForm.astro"
      to: "Netlify Forms"
      via: "data-netlify attribute"
      pattern: 'data-netlify="true"'
    - from: "src/components/Footer.astro"
      to: "src/components/NewsletterSignup.astro"
      via: "import and render with compact prop"
      pattern: "NewsletterSignup compact"
---

<objective>
Build the contact page with Netlify Forms, success page, client-side validation, and integrate NewsletterSignup into the site footer and homepage.

Purpose: Visitors can send messages via the contact form and subscribe to the newsletter from any page (footer). The disabled homepage newsletter placeholder is replaced with the live component.
Output: 3 new files (contact page, ContactForm component, success page) + 2 modified files (Footer, index.astro)
</objective>

<execution_context>
@/Users/dawoodkamar/projects/Dawood/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dawoodkamar/projects/Dawood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pages-features/03-RESEARCH.md
@.planning/phases/03-pages-features/03-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title: string;
  description?: string;
}
```

From src/components/NewsletterSignup.astro (created in Plan 03-01):
```astro
interface Props {
  compact?: boolean;  // When true, omits heading and description text
}
```
Usage: `<NewsletterSignup />` for full version, `<NewsletterSignup compact />` for footer

From src/components/Footer.astro (current state):
- No imports, no frontmatter logic
- Simple footer with copyright, nav links, social links (YouTube, LinkedIn)
- Max width: `max-w-4xl`, padding: `px-4 py-8`

From src/pages/index.astro (Section 5 to replace — lines 106-129):
- Currently a disabled `<form>` with `<input disabled>` and `<button disabled>Coming Soon</button>`
- Section class: `rounded-lg bg-gray-50 py-12 text-center dark:bg-gray-900`
- Must be replaced with `<NewsletterSignup />` component (non-compact)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ContactForm.astro, contact page, and success page</name>
  <files>src/components/ContactForm.astro, src/pages/contact.astro, src/pages/success.astro</files>

  <read_first>
    - src/layouts/BaseLayout.astro (Props interface, body classes, main element max-width)
    - src/components/Navigation.astro (astro:page-load script pattern for client-side JS)
    - src/components/Footer.astro (social link URLs — YouTube and LinkedIn — to reuse on contact page)
    - .planning/phases/03-pages-features/03-RESEARCH.md (Pattern 4: Contact Form with Netlify Forms)
  </read_first>

  <action>
    Create `src/components/ContactForm.astro` (CONTACT-02, CONTACT-04, CONTACT-05):

    - No frontmatter logic needed (pure HTML + script)
    - Form element with ALL of these attributes: `<form name="contact" method="POST" action="/success/" data-netlify="true" netlify-honeypot="bot-field" data-astro-reload class="mt-8 space-y-6">`
    - Hidden form-name input (REQUIRED for Netlify static site forms): `<input type="hidden" name="form-name" value="contact" />`
    - Honeypot field (hidden from humans, catches bots): `<p class="hidden" aria-hidden="true"><label>Do not fill this field: <input name="bot-field" tabindex="-1" autocomplete="off" /></label></p>`
    - Name field: `<div><label for="name" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Name</label><input type="text" id="name" name="name" required class="mt-1 block w-full rounded-lg border border-gray-300 px-4 py-2 text-sm dark:border-gray-700 dark:bg-gray-800 dark:text-gray-100" /></div>`
    - Email field: `<div><label for="email" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Email</label><input type="email" id="email" name="email" required class="mt-1 block w-full rounded-lg border border-gray-300 px-4 py-2 text-sm dark:border-gray-700 dark:bg-gray-800 dark:text-gray-100" /></div>`
    - Message field: `<div><label for="message" class="block text-sm font-medium text-gray-700 dark:text-gray-300">Message</label><textarea id="message" name="message" rows="5" required class="mt-1 block w-full rounded-lg border border-gray-300 px-4 py-2 text-sm dark:border-gray-700 dark:bg-gray-800 dark:text-gray-100"></textarea></div>`
    - Submit button: `<button type="submit" class="rounded-lg bg-blue-600 px-6 py-3 text-sm font-semibold text-white hover:bg-blue-700">Send Message</button>`
    - Client-side validation script (CONTACT-05): Wrap in `<script>` using `document.addEventListener("astro:page-load", () => { ... })` pattern:
      - Select the form: `const form = document.querySelector('form[name="contact"]');`
      - On submit, prevent default if any required field is empty or email is invalid
      - Email regex check: `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
      - For each field (name, email, message), check value.trim() length; if empty, add a `<p class="mt-1 text-sm text-red-600">` error message after the input and set `aria-invalid="true"` on the input
      - Clear previous error messages at the start of each submit handler run
      - If validation passes, call `form.submit()` to trigger native form submission (which will respect `data-astro-reload`)

    Create `src/pages/contact.astro` (CONTACT-01, CONTACT-06):

    - Import: `import BaseLayout from "../layouts/BaseLayout.astro";` and `import ContactForm from "../components/ContactForm.astro";`
    - Wrap in `<BaseLayout title="Contact — Dawood Kamar" description="Get in touch with Dawood Kamar.">`
    - Page heading: `<h1 class="text-3xl font-bold md:text-4xl">Contact</h1>`
    - Intro paragraph: `<p class="mt-4 text-gray-600 dark:text-gray-400">Have a question, idea, or just want to say hello? Send me a message or find me on social media.</p>`
    - Mailto link: `<p class="mt-2"><a href="mailto:hello@dawoodkamar.com" class="text-blue-600 hover:underline dark:text-blue-400">hello@dawoodkamar.com</a></p>`
    - Render `<ContactForm />`
    - Social links section after form (CONTACT-06): `<div class="mt-12 border-t border-gray-200 pt-8 dark:border-gray-800"><h2 class="text-xl font-semibold">Find Me Online</h2><div class="mt-4 flex gap-4">` with two links:
      - `<a href="https://youtube.com/@dawoodkamar" target="_blank" rel="noopener noreferrer" class="rounded-lg border border-gray-200 px-4 py-2 text-sm hover:bg-gray-50 dark:border-gray-800 dark:hover:bg-gray-800">YouTube</a>`
      - `<a href="https://linkedin.com/in/dawood-kamar" target="_blank" rel="noopener noreferrer" class="rounded-lg border border-gray-200 px-4 py-2 text-sm hover:bg-gray-50 dark:border-gray-800 dark:hover:bg-gray-800">LinkedIn</a>`

    Create `src/pages/success.astro` (CONTACT-03):

    - Import: `import BaseLayout from "../layouts/BaseLayout.astro";`
    - Wrap in `<BaseLayout title="Message Sent — Dawood Kamar" description="Your message has been sent successfully.">`
    - Centered content: `<div class="py-16 text-center">`
    - Heading: `<h1 class="text-3xl font-bold">Message Sent</h1>`
    - Paragraph: `<p class="mt-4 text-gray-600 dark:text-gray-400">Thanks for reaching out. I'll get back to you soon.</p>`
    - Back link: `<a href="/" class="mt-8 inline-block rounded-lg bg-blue-600 px-6 py-3 text-sm font-semibold text-white hover:bg-blue-700">Back to Home</a>`
  </action>

  <verify>
    <automated>PATH="/opt/homebrew/opt/node@22/bin:$PATH" npm run build 2>&1 | tail -5 && ls dist/contact/index.html dist/success/index.html 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - File `src/components/ContactForm.astro` exists
    - File `src/pages/contact.astro` exists
    - File `src/pages/success.astro` exists
    - `grep -c 'data-netlify="true"' src/components/ContactForm.astro` returns 1
    - `grep -c 'data-astro-reload' src/components/ContactForm.astro` returns 1
    - `grep -c 'netlify-honeypot="bot-field"' src/components/ContactForm.astro` returns 1
    - `grep -c 'name="form-name" value="contact"' src/components/ContactForm.astro` returns 1
    - `grep -c 'name="bot-field"' src/components/ContactForm.astro` returns 1
    - `grep -c 'action="/success/"' src/components/ContactForm.astro` returns 1
    - `grep -c 'type="email"' src/components/ContactForm.astro` returns 1
    - `grep -c 'astro:page-load' src/components/ContactForm.astro` returns 1
    - `grep -c "ContactForm" src/pages/contact.astro` returns at least 2 (import + usage)
    - `grep -c "youtube.com/@dawoodkamar" src/pages/contact.astro` returns 1
    - `grep -c "linkedin.com/in/dawood-kamar" src/pages/contact.astro` returns 1
    - `grep -c "BaseLayout" src/pages/success.astro` returns at least 1
    - `grep -c "Back to Home" src/pages/success.astro` returns 1
    - `npm run build` exits 0
    - `dist/contact/index.html` exists after build
    - `dist/success/index.html` exists after build
    - `grep -c "data-netlify" dist/contact/index.html` returns at least 1
  </acceptance_criteria>

  <done>Contact page at /contact with Netlify Forms (data-netlify, data-astro-reload, honeypot, hidden form-name), client-side validation, social links. Success page at /success with thank-you message and home link.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate NewsletterSignup into site footer and replace homepage placeholder</name>
  <files>src/components/Footer.astro, src/pages/index.astro</files>

  <read_first>
    - src/components/Footer.astro (current footer — must add NewsletterSignup import and render)
    - src/pages/index.astro (current homepage — Section 5 newsletter placeholder to replace, lines 106-129)
    - src/components/NewsletterSignup.astro (created in Plan 03-01 — verify compact prop and full version markup)
  </read_first>

  <action>
    Modify `src/components/Footer.astro` (NEWS-04 — footer location):

    - Add frontmatter block with import: `---\nimport NewsletterSignup from "./NewsletterSignup.astro";\n---`
    - Insert `<NewsletterSignup compact />` BEFORE the existing footer content (before the `<div class="mx-auto flex max-w-4xl...">` element)
    - The compact version renders only the form (no heading/description), so wrap it: `<div class="mx-auto max-w-4xl px-4 pt-8"><NewsletterSignup compact /></div>`
    - Keep ALL existing footer content (copyright, nav links, social links) unchanged below it

    Modify `src/pages/index.astro` (NEWS-04 — homepage location):

    - Add import in frontmatter: `import NewsletterSignup from "../components/NewsletterSignup.astro";`
    - Replace the ENTIRE Section 5 (lines 106-129, the `<!-- Section 5: Newsletter Placeholder -->` block with the disabled form, "Coming Soon" button, and "Newsletter launching soon" text) with: `<!-- Section 5: Newsletter -->\n<NewsletterSignup />`
    - The non-compact `<NewsletterSignup />` renders its own section wrapper with heading "Stay in the Loop", description, and the live form — matching the existing visual style but with a working form
    - Do NOT change any other sections (hero, positioning, pillars, featured posts)
  </action>

  <verify>
    <automated>PATH="/opt/homebrew/opt/node@22/bin:$PATH" npm run build 2>&1 | tail -5 && grep -c "embed-subscribe" dist/index.html && grep -c "embed-subscribe" dist/blog/index.html && echo "Footer check:" && grep -c "embed-subscribe" dist/contact/index.html</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "NewsletterSignup" src/components/Footer.astro` returns at least 2 (import + usage)
    - `grep -c "compact" src/components/Footer.astro` returns at least 1
    - `grep -c "NewsletterSignup" src/pages/index.astro` returns at least 2 (import + usage)
    - `grep -c "disabled" src/pages/index.astro` returns 0 (no more disabled placeholder)
    - `grep -c "Coming Soon" src/pages/index.astro` returns 0 (placeholder text removed)
    - `grep -c "embed-subscribe" dist/index.html` returns at least 2 (homepage section + footer)
    - `grep -c "embed-subscribe" dist/contact/index.html` returns at least 1 (footer)
    - `grep -c "embed-subscribe" dist/blog/ai-noise/index.html` returns at least 2 (post footer + site footer)
    - NewsletterSignup appears in 3+ locations: homepage section, each blog post page, site footer on every page
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>NewsletterSignup component appears in at least 3 locations: homepage section (replacing disabled placeholder), end of each blog post (from Plan 03-01), and site footer (compact version). All locations POST to Buttondown embed endpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify contact form, newsletter integration, and full site in browser</name>
  <what-built>Contact page with Netlify Forms, success page, client-side validation, NewsletterSignup in site footer and homepage</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:4321/contact
    2. Verify: contact form shows Name, Email, Message fields with labels
    3. Try submitting the empty form — verify client-side validation shows error messages
    4. Try submitting with an invalid email — verify email validation error
    5. Verify: YouTube and LinkedIn social links appear below the form
    6. Visit http://localhost:4321/ (homepage)
    7. Verify: Section 5 newsletter form is LIVE (no "Coming Soon" or disabled state) — shows "Subscribe" button
    8. Scroll to footer — verify a compact newsletter signup (email input + subscribe button, no heading) appears in the footer
    9. Visit http://localhost:4321/blog/ — scroll to footer — verify newsletter signup also in footer here
    10. Visit a blog post — verify newsletter signup appears both after the post content AND in the footer
    11. Test dark mode on contact page and newsletter components — verify correct colors
    12. View page source on /contact — verify `data-netlify="true"`, `data-astro-reload`, and hidden `form-name` input are present
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `PATH="/opt/homebrew/opt/node@22/bin:$PATH" npm run build` exits 0
- `ls dist/contact/index.html dist/success/index.html` confirms both pages generated
- `grep "data-netlify" dist/contact/index.html` confirms Netlify Forms attributes present
- `grep "data-astro-reload" dist/contact/index.html` confirms view transition bypass
- `grep "form-name" dist/contact/index.html` confirms hidden form-name input
- `grep "bot-field" dist/contact/index.html` confirms honeypot field
- `grep -r "embed-subscribe" dist/ | wc -l` returns 5+ (homepage, 2 blog posts in footer + post body, contact footer, blog listing footer)
</verification>

<success_criteria>
- /contact page shows form with Name, Email, Message fields, data-netlify, data-astro-reload, honeypot
- /success page shows thank-you message with link home
- Client-side validation prevents empty submissions and invalid emails
- Social links (YouTube, LinkedIn) visible on contact page
- NewsletterSignup in 3+ locations: homepage section, blog post footer, site footer
- Disabled newsletter placeholder on homepage is fully replaced
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-pages-features/03-02-SUMMARY.md`
</output>
