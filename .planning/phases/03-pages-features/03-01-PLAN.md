---
phase: 03-pages-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/blog/index.astro
  - src/pages/blog/[id].astro
  - src/components/PostNavigation.astro
  - src/components/NewsletterSignup.astro
autonomous: false
requirements: [BLOG-01, BLOG-02, BLOG-03, BLOG-04, BLOG-05, BLOG-06, BLOG-07, NEWS-01, NEWS-02, NEWS-03, NEWS-04]

must_haves:
  truths:
    - "Visiting /blog/ shows all non-draft posts in reverse chronological order"
    - "Each post card shows title, description, date, reading time, and category tag"
    - "Visiting /blog/ai-noise/ renders the full markdown post with typography styles"
    - "Previous/Next links appear at the bottom of each post page"
    - "NewsletterSignup form appears at the end of each blog post"
    - "NewsletterSignup form POSTs to Buttondown embed endpoint"
    - "Draft posts are excluded from /blog listing and getStaticPaths in production builds"
  artifacts:
    - path: "src/pages/blog/index.astro"
      provides: "Blog listing page with post cards"
      contains: "getCollection"
    - path: "src/pages/blog/[id].astro"
      provides: "Individual post page with dynamic routing"
      contains: "getStaticPaths"
    - path: "src/components/PostNavigation.astro"
      provides: "Previous/Next post navigation"
      contains: "prevPost"
    - path: "src/components/NewsletterSignup.astro"
      provides: "Buttondown newsletter embed form"
      contains: "embed-subscribe"
  key_links:
    - from: "src/pages/blog/index.astro"
      to: "astro:content"
      via: "getCollection and render imports"
      pattern: 'import.*getCollection.*render.*from "astro:content"'
    - from: "src/pages/blog/[id].astro"
      to: "src/components/PostNavigation.astro"
      via: "import and props passing"
      pattern: "PostNavigation.*prevPost.*nextPost"
    - from: "src/pages/blog/[id].astro"
      to: "src/components/NewsletterSignup.astro"
      via: "import and render after article"
      pattern: "NewsletterSignup"
    - from: "src/components/NewsletterSignup.astro"
      to: "Buttondown API"
      via: "form action attribute"
      pattern: "buttondown.com/api/emails/embed-subscribe"
---

<objective>
Build the blog listing page, individual post pages, PostNavigation component, and NewsletterSignup component.

Purpose: Visitors can browse all posts, read full articles with typography styling, navigate between posts, and subscribe to the newsletter from each post page.
Output: 4 files — blog index, dynamic post route, PostNavigation, NewsletterSignup
</objective>

<execution_context>
@/Users/dawoodkamar/projects/Dawood/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dawoodkamar/projects/Dawood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pages-features/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title: string;
  description?: string;
}
```
Usage: `<BaseLayout title="..." description="...">` wraps all pages.

From src/content.config.ts:
```typescript
schema: z.object({
  title: z.string(),
  description: z.string(),
  date: z.coerce.date(),
  category: z.enum(["AI & Work", "Focus & Discipline", "Culture & Place", "Clarity & Communication"]),
  tags: z.array(z.string()).default([]),
  draft: z.boolean().default(false),
})
```

From src/pages/index.astro (established patterns):
```typescript
import { getCollection, render } from "astro:content";
// Draft filter pattern:
const allPosts = await getCollection("blog", ({ data }) =>
  import.meta.env.PROD ? data.draft !== true : true
);
// Reading time pattern:
const { remarkPluginFrontmatter } = await render(post);
// minutesRead: remarkPluginFrontmatter.minutesRead
// URL pattern: /blog/${post.id}/
```

From src/config.ts:
```typescript
export const SITE_URL = "https://dawoodkamar.com";
export const SITE_TITLE = "Dawood Kamar";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NewsletterSignup.astro and PostNavigation.astro components</name>
  <files>src/components/NewsletterSignup.astro, src/components/PostNavigation.astro</files>

  <read_first>
    - src/components/Footer.astro (for Tailwind class patterns and dark mode styling conventions)
    - src/pages/index.astro (for the existing newsletter placeholder form that this component replaces)
    - src/content.config.ts (for the blog collection schema — PostNavigation needs to reference CollectionEntry type)
    - .planning/phases/03-pages-features/03-RESEARCH.md (Pattern 3: NewsletterSignup, Pattern 5: PostNavigation)
  </read_first>

  <action>
    Create `src/components/NewsletterSignup.astro`:

    - Accept an optional `compact` boolean prop (default `false`). When `compact` is true, omit the heading and description (used in footer placement per NEWS-04). Interface: `interface Props { compact?: boolean; }`
    - The `<form>` element must have these exact attributes:
      - `action="https://buttondown.com/api/emails/embed-subscribe/dawoodkamar"`
      - `method="post"`
      - `target="_blank"` (opens Buttondown thank-you page in new tab — simplest success feedback per NEWS-03)
    - Include a hidden input: `<input type="hidden" name="embed" value="1" />`
    - Include an email input: `<input type="email" name="email" placeholder="your@email.com" required aria-label="Email address" class="flex-1 rounded-lg border border-gray-300 px-4 py-2 text-sm dark:border-gray-700 dark:bg-gray-800 dark:text-gray-100" />`
    - Include a submit button: `<button type="submit" class="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700">Subscribe</button>`
    - When `compact` is false, wrap with section containing heading "Stay in the Loop" and paragraph "Writing on AI, focus, and meaningful work. No noise." — matching the existing homepage placeholder text and styling
    - Outer section (non-compact): `<section class="rounded-lg bg-gray-50 py-12 text-center dark:bg-gray-900">`
    - Form wrapper: `<form class="mx-auto mt-6 flex max-w-md gap-2" ...>`
    - Add a comment at the top of the frontmatter: `// TODO: Confirm Buttondown username is "dawoodkamar" — update if different`

    Create `src/components/PostNavigation.astro`:

    - Import type: `import type { CollectionEntry } from "astro:content";`
    - Interface: `interface Props { prevPost: CollectionEntry<"blog"> | null; nextPost: CollectionEntry<"blog"> | null; }`
    - Destructure: `const { prevPost, nextPost } = Astro.props;`
    - Only render the `<nav>` element if at least one of prevPost/nextPost is not null: `{(prevPost || nextPost) && (<nav>...</nav>)}`
    - Nav element: `<nav class="mt-12 flex items-center justify-between border-t border-gray-200 pt-6 dark:border-gray-800">`
    - Left side (prevPost — older, labeled "Previous"): `<a href={'/blog/' + prevPost.id + '/'} class="group flex flex-col"><span class="text-sm text-gray-500 dark:text-gray-400">Previous</span><span class="font-medium group-hover:text-blue-600 dark:group-hover:text-blue-400">{prevPost.data.title}</span></a>`
    - Right side (nextPost — newer, labeled "Next"): same pattern but with `text-right ml-auto` classes
    - If prevPost is null, render an empty `<div></div>` as spacer; same for nextPost
  </action>

  <verify>
    <automated>PATH="/opt/homebrew/opt/node@22/bin:$PATH" npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File `src/components/NewsletterSignup.astro` exists
    - File `src/components/PostNavigation.astro` exists
    - `grep -c "embed-subscribe/dawoodkamar" src/components/NewsletterSignup.astro` returns 1
    - `grep -c 'method="post"' src/components/NewsletterSignup.astro` returns 1
    - `grep -c 'target="_blank"' src/components/NewsletterSignup.astro` returns 1
    - `grep -c 'name="embed" value="1"' src/components/NewsletterSignup.astro` returns 1
    - `grep -c 'type="email"' src/components/NewsletterSignup.astro` returns 1
    - `grep -c "required" src/components/NewsletterSignup.astro` returns at least 1
    - `grep -c "compact" src/components/NewsletterSignup.astro` returns at least 1
    - `grep -c "CollectionEntry" src/components/PostNavigation.astro` returns at least 1
    - `grep -c "prevPost" src/components/PostNavigation.astro` returns at least 2
    - `grep -c "nextPost" src/components/PostNavigation.astro` returns at least 2
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>NewsletterSignup.astro POSTs to Buttondown embed endpoint with email validation; PostNavigation.astro renders conditional prev/next links with CollectionEntry types</done>
</task>

<task type="auto">
  <name>Task 2: Create blog listing page and individual post page</name>
  <files>src/pages/blog/index.astro, src/pages/blog/[id].astro</files>

  <read_first>
    - src/pages/index.astro (for established getCollection + render + draft filter + reading time patterns)
    - src/components/NewsletterSignup.astro (just created in Task 1 — verify props interface)
    - src/components/PostNavigation.astro (just created in Task 1 — verify props interface)
    - src/layouts/BaseLayout.astro (Props interface: title required, description optional)
    - src/content.config.ts (schema field names: title, description, date, category, tags, draft)
    - .planning/phases/03-pages-features/03-RESEARCH.md (Pattern 1: Blog Listing, Pattern 2: Individual Post)
  </read_first>

  <action>
    Create `src/pages/blog/index.astro` (BLOG-01, BLOG-02, BLOG-07):

    - Frontmatter imports: `import BaseLayout from "../../layouts/BaseLayout.astro";` and `import { getCollection, render } from "astro:content";`
    - Query all posts with draft filter (same pattern as index.astro): `const posts = (await getCollection("blog", ({ data }) => import.meta.env.PROD ? data.draft !== true : true)).sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());`
    - Get reading time for all posts: `const postsWithMeta = await Promise.all(posts.map(async (post) => { const { remarkPluginFrontmatter } = await render(post); return { post, minutesRead: remarkPluginFrontmatter.minutesRead }; }));`
    - Wrap in `<BaseLayout title="Blog — Dawood Kamar" description="Writing on AI, focus, and building a meaningful career.">`
    - Page heading: `<h1 class="text-3xl font-bold md:text-4xl">Blog</h1>`
    - Subtitle: `<p class="mt-2 text-gray-600 dark:text-gray-400">All posts, newest first.</p>`
    - Post grid: `<div class="mt-8 grid gap-6">`
    - Each post card rendered as `<article class="rounded-lg border border-gray-200 p-6 dark:border-gray-800">` containing:
      - Date + reading time row: `<div class="flex items-center gap-2 text-sm text-gray-500 dark:text-gray-400">` with `<time datetime={post.data.date.toISOString()}>{post.data.date.toLocaleDateString("en-GB", { day: "numeric", month: "short", year: "numeric" })}</time>`, a `<span>·</span>` separator, and `<span>{minutesRead}</span>`
      - Title as link: `<h2 class="mt-2 text-xl font-semibold"><a href={'/blog/' + post.id + '/'} class="hover:text-blue-600 dark:hover:text-blue-400">{post.data.title}</a></h2>`
      - Description: `<p class="mt-2 text-gray-600 dark:text-gray-400">{post.data.description}</p>`
      - Category badge: `<span class="mt-3 inline-block rounded-full bg-gray-100 px-3 py-1 text-xs font-medium text-gray-700 dark:bg-gray-800 dark:text-gray-300">{post.data.category}</span>`

    Create `src/pages/blog/[id].astro` (BLOG-03, BLOG-04, BLOG-05, BLOG-06, BLOG-07):

    - Frontmatter imports: `import BaseLayout from "../../layouts/BaseLayout.astro";`, `import { getCollection, render } from "astro:content";`, `import PostNavigation from "../../components/PostNavigation.astro";`, `import NewsletterSignup from "../../components/NewsletterSignup.astro";`
    - `getStaticPaths` function: filter drafts in production, sort by date descending, map to `{ params: { id: post.id }, props: { post, prevPost: posts[index + 1] ?? null, nextPost: posts[index - 1] ?? null } }`
    - NOTE: `posts[index + 1]` is the OLDER post (Previous), `posts[index - 1]` is the NEWER post (Next) because array is sorted newest-first
    - Destructure props: `const { post, prevPost, nextPost } = Astro.props;`
    - Render: `const { Content, remarkPluginFrontmatter } = await render(post);` then `const minutesRead = remarkPluginFrontmatter.minutesRead;`
    - Wrap in `<BaseLayout title={post.data.title} description={post.data.description}>`
    - Article header: `<article><header class="mb-8">` containing:
      - Title: `<h1 class="text-3xl font-bold md:text-4xl">{post.data.title}</h1>`
      - Meta row: `<div class="mt-4 flex flex-wrap items-center gap-2 text-sm text-gray-500 dark:text-gray-400">` with `<time datetime={post.data.date.toISOString()}>{post.data.date.toLocaleDateString("en-GB", { dateStyle: "long" })}</time>`, separator, `<span>{minutesRead}</span>`
      - Category badge: `<span class="mt-2 inline-block rounded-full bg-gray-100 px-3 py-1 text-xs font-medium text-gray-700 dark:bg-gray-800 dark:text-gray-300">{post.data.category}</span>`
    - Content wrapper with typography: `<div class="prose dark:prose-invert max-w-none"><Content /></div>`
    - Close `</article>`
    - After article: `<PostNavigation prevPost={prevPost} nextPost={nextPost} />`
    - After PostNavigation: `<div class="mt-12"><NewsletterSignup /></div>`
  </action>

  <verify>
    <automated>PATH="/opt/homebrew/opt/node@22/bin:$PATH" npm run build 2>&1 | tail -5 && ls dist/blog/index.html dist/blog/ai-noise/index.html dist/blog/focus-advantage/index.html 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - File `src/pages/blog/index.astro` exists
    - File `src/pages/blog/[id].astro` exists
    - `grep -c "getStaticPaths" src/pages/blog/[id].astro` returns 1
    - `grep -c 'params: { id: post.id }' src/pages/blog/[id].astro` returns 1
    - `grep -c 'import { getCollection, render } from "astro:content"' src/pages/blog/[id].astro` returns 1
    - `grep -c 'import { getCollection, render } from "astro:content"' src/pages/blog/index.astro` returns 1
    - `grep -c "prose dark:prose-invert" src/pages/blog/[id].astro` returns 1
    - `grep -c "PostNavigation" src/pages/blog/[id].astro` returns at least 2 (import + usage)
    - `grep -c "NewsletterSignup" src/pages/blog/[id].astro` returns at least 2 (import + usage)
    - `grep -c "data.draft !== true" src/pages/blog/index.astro` returns 1
    - `grep -c "data.draft !== true" src/pages/blog/[id].astro` returns 1
    - `grep -c "minutesRead" src/pages/blog/index.astro` returns at least 2
    - `grep -c "post.data.category" src/pages/blog/index.astro` returns at least 1
    - `grep -c "post.data.category" src/pages/blog/[id].astro` returns at least 1
    - `npm run build` exits 0
    - `dist/blog/index.html` exists after build
    - `dist/blog/ai-noise/index.html` exists after build
    - `dist/blog/focus-advantage/index.html` exists after build
    - `grep -c "embed-subscribe" dist/blog/ai-noise/index.html` returns at least 1 (NewsletterSignup rendered)
  </acceptance_criteria>

  <done>Blog listing page at /blog shows all non-draft posts with title, description, date, reading time, and category badge. Individual post pages render full markdown with prose typography, prev/next navigation, and newsletter signup. Draft posts are excluded from getStaticPaths in production.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify blog pages and newsletter component in browser</name>
  <what-built>Blog listing page (/blog), individual post pages (/blog/ai-noise/, /blog/focus-advantage/), PostNavigation prev/next links, NewsletterSignup form on each post page</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:4321/blog/
    2. Verify: page shows post cards with title, description excerpt, date, reading time (e.g. "2 min read"), and category badge
    3. Verify: posts are in reverse chronological order (newest first)
    4. Click on a post title to navigate to the individual post page
    5. Verify: full markdown content renders with proper heading sizes, paragraph spacing, and code block styling (prose typography)
    6. Verify: category badge appears in the post header
    7. Scroll to bottom of post — verify Previous/Next navigation links with post titles
    8. Verify: NewsletterSignup form appears below post navigation with "Subscribe" button
    9. Test dark mode toggle — verify all blog elements have correct dark mode colors
    10. Test navigation: click "Blog" in nav bar, then navigate to a post, then use browser back — verify view transitions work smoothly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `PATH="/opt/homebrew/opt/node@22/bin:$PATH" npm run build` exits 0
- `ls dist/blog/index.html` confirms blog listing page generated
- `ls dist/blog/ai-noise/index.html dist/blog/focus-advantage/index.html` confirms post pages generated
- `grep "embed-subscribe" dist/blog/ai-noise/index.html` confirms NewsletterSignup rendered on post pages
- `grep "Previous\|Next" dist/blog/ai-noise/index.html` confirms PostNavigation rendered
- Draft post (`switzerland-ambition.md` if `draft: true`) does NOT have a generated page in dist/blog/
</verification>

<success_criteria>
- /blog lists all non-draft posts reverse-chronologically with title, description, date, reading time, category
- /blog/[id] renders full markdown with @tailwindcss/typography prose styling
- PostNavigation shows prev/next links at bottom of each post
- NewsletterSignup appears at end of each post and POSTs to Buttondown embed endpoint
- Draft posts are excluded from both listing and static path generation in production
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-pages-features/03-01-SUMMARY.md`
</output>
