---
phase: 04-seo-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Navigation.astro
  - src/components/BaseHead.astro
  - src/pages/contact.astro
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SEO-01, SEO-09, RESP-03]
gap_closure: true

must_haves:
  truths:
    - "Mobile hamburger dropdown does not contain a theme toggle (icon already visible in header at all sizes)"
    - "og:image meta tag outputs an absolute URL (https://dawoodkamar.com/og-default.png) in built HTML"
    - "Contact page social links (YouTube, LinkedIn) meet 44px minimum tap target height"
    - "REQUIREMENTS.md SEO-09 text matches the actual system font implementation"
  artifacts:
    - path: "src/components/Navigation.astro"
      provides: "Mobile nav without redundant theme toggle"
      contains: "mobile-menu"
    - path: "src/components/BaseHead.astro"
      provides: "Absolute og:image URL via Astro.site resolution"
      contains: "new URL"
    - path: "src/pages/contact.astro"
      provides: "Social links with py-3 padding for 44px tap targets"
      contains: "py-3"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SEO-09 updated to reflect system font decision"
      contains: "system font stack"
  key_links:
    - from: "src/components/BaseHead.astro"
      to: "og:image meta tag"
      via: "new URL(image, Astro.site)"
      pattern: "new URL\\(image.*Astro\\.site\\)"
---

<objective>
Close four gaps identified in VERIFICATION.md and UAT.md for phase 04.

Purpose: Resolve all remaining cosmetic and documentation issues so the phase can pass re-verification.
Output: Four small fixes across four files — no new features, just corrections.
</objective>

<execution_context>
@/Users/dawoodkamar/projects/Dawood/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dawoodkamar/projects/Dawood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-polish/04-VERIFICATION.md

<interfaces>
<!-- Current state of files to modify -->

From src/components/Navigation.astro (lines 78-88 — mobile theme toggle to remove):
```astro
<li>
  <button
    id="mobile-theme-toggle"
    type="button"
    aria-label="Toggle dark mode"
    class="flex items-center gap-2 py-3 hover:text-blue-600 dark:hover:text-blue-400"
  >
    <span class="dark:hidden">Dark mode</span>
    <span class="hidden dark:inline">Light mode</span>
  </button>
</li>
```

From src/components/BaseHead.astro (line 8 — image default):
```astro
const { title, description, image = '/og-default.png' } = Astro.props;
```

From src/pages/contact.astro (lines 17-18 — social links with py-2):
```astro
<a href="https://youtube.com/@dawoodkamar" ... class="rounded-lg border border-gray-200 px-4 py-2 text-sm ...">YouTube</a>
<a href="https://linkedin.com/in/dawood-kamar" ... class="rounded-lg border border-gray-200 px-4 py-2 text-sm ...">LinkedIn</a>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant mobile theme toggle and fix og:image URL</name>
  <files>src/components/Navigation.astro, src/components/BaseHead.astro</files>
  <action>
    **Navigation.astro — Remove mobile theme toggle (UAT gap):**
    1. Delete the entire `<li>` block containing `id="mobile-theme-toggle"` (lines 78-88) from the mobile menu `<ul>`.
    2. In the `<script>` section, remove the `mobileThemeToggle` constant declaration (line 98) and its event listener block (lines 116-118: `if (mobileThemeToggle) { ... }`). Keep the `themeToggle` (desktop/header) listener intact.

    **BaseHead.astro — Resolve og:image to absolute URL (VERIFICATION gap 1):**
    1. After the existing `canonicalURL` line, add a new line that resolves the image to an absolute URL:
       `const imageURL = new URL(image, Astro.site).toString();`
    2. Replace `content={image}` with `content={imageURL}` in both the `og:image` meta tag (line 22) and the `twitter:image` meta tag (line 28).
    3. Update the interface comment on line 5 from "absolute URL" to "path or absolute URL — resolved to absolute via Astro.site".
  </action>
  <verify>
    <automated>cd /Users/dawoodkamar/projects/Dawood && npm run build 2>&1 | tail -5 && grep 'mobile-theme-toggle' dist/index.html; echo "exit: $?" && grep 'og:image' dist/index.html | head -1</automated>
  </verify>
  <done>
    - Build succeeds with no errors
    - `mobile-theme-toggle` does not appear in built HTML (grep returns no match)
    - `og:image` content value is `https://dawoodkamar.com/og-default.png` (absolute URL)
    - Desktop/header theme toggle still functions (id="theme-toggle" still present)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix contact social link tap targets and update REQUIREMENTS.md</name>
  <files>src/pages/contact.astro, .planning/REQUIREMENTS.md</files>
  <action>
    **contact.astro — Fix tap targets (VERIFICATION gap 2, RESP-03):**
    1. On both social link anchors (YouTube and LinkedIn in "Find Me Online" section, lines 17-18), change `py-2` to `py-3` to bring effective height from ~36px to ~44px.

    **REQUIREMENTS.md — Align SEO-09 with implementation (VERIFICATION gap 3):**
    1. Find the line `- [x] **SEO-09**: Google Fonts loaded with \`display=swap\` + preconnect, or via Astro Fonts API`
    2. Replace it with: `- [x] **SEO-09**: No render-blocking fonts — system font stack used (no web font loading); if web fonts are added later, use \`display=swap\` + preconnect`
  </action>
  <verify>
    <automated>cd /Users/dawoodkamar/projects/Dawood && grep 'py-2' src/pages/contact.astro; echo "py-2 check exit: $?" && grep 'py-3' src/pages/contact.astro && grep 'SEO-09' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - No `py-2` remains in contact.astro social links (grep returns no match)
    - Both social links now use `py-3` (~44px effective height)
    - REQUIREMENTS.md SEO-09 reads "No render-blocking fonts — system font stack used" instead of "Google Fonts loaded with display=swap"
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes with no errors
2. `grep 'mobile-theme-toggle' dist/index.html` returns no match
3. `grep 'og:image' dist/index.html` shows absolute URL `https://dawoodkamar.com/og-default.png`
4. `grep 'py-2' src/pages/contact.astro` returns no match in social links section
5. `grep 'SEO-09' .planning/REQUIREMENTS.md` shows updated text about system fonts
</verification>

<success_criteria>
All four gaps from VERIFICATION.md and UAT.md are resolved:
- Mobile hamburger menu no longer contains a redundant theme toggle
- og:image outputs an absolute URL for social crawlers
- Contact page social links meet 44px minimum tap target
- REQUIREMENTS.md SEO-09 matches actual system font implementation
Build succeeds, no regressions introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-polish/04-03-SUMMARY.md`
</output>
